-# Hardcoded examples for now, replace with loop later
%h1 Admin/Review
- @video_lists.each do |video_list|
  %h2= video_list.name
  %ul
    - video_list.videos.each do |video|
      %li= link_to video.name,'api_admin/show'
